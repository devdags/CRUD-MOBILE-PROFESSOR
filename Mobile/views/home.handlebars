<div style="position:relative; margin-left:auto; margin-right:auto; width: 350px;">
<h1>DEATH NOTE</h1><br><br>
<h1>Lista de Nomes: </h1>

{{#each posts}}
    <h1>{{dataValues.titulo}}</h1>
    <p>{{dataValues.conteudo}}</p>
    <small>{{dataValues.createdAt}}</small>
<a href="/deletar/{{dataValues.id}}"><button>Deletar</button><a/>
<a href="/editar/{{dataValues.id}}/update"><button>Editar</button></a>
<hr>
{{/each}}

<form method="GET">
  <button type="submit" formaction="/cad">Nova Vitima</button>
</form>
</div>
